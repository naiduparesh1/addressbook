pipeline {
    agent any

    stages {
        stage('compile') {
            steps {
                echo 'compiling the java based code'
            }
        }
        stage('Unittest') {
            steps {
                echo 'testing the code'
            }
        }
        stage('package') {
            steps {
                echo 'packaging the code'
            }
        }
    }
}
